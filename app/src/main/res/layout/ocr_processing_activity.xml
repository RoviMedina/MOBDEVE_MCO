<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- TODO: Create OcrProcessingActivity.kt and show this while OCR scans a captured/gallery receipt image. -->
    <ProgressBar
        android:id="@+id/progressOcr"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:indeterminate="true" />

    <TextView
        android:id="@+id/tvOcrTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Reading Receipt"
        android:textColor="@color/textPrimary"
        android:textSize="26sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tvOcrMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="Extracting store name, date, total, and line items."
        android:textColor="@color/textSecondary"
        android:textSize="15sp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnRetryOcr"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="32dp"
        android:text="@string/retry_scan"
        android:textAllCaps="false"
        android:visibility="gone"
        app:cornerRadius="18dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnManualEntry"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="12dp"
        android:text="@string/manual_entry"
        android:textAllCaps="false"
        android:visibility="gone"
        app:cornerRadius="18dp" />

    <!-- TODO: On OCR success, pass extracted fields to ReviewReceiptActivity. On failure, show retry/manual entry. -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCancelOcr"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="12dp"
        android:text="Cancel"
        android:textAllCaps="false"
        app:cornerRadius="18dp" />
</LinearLayout>
